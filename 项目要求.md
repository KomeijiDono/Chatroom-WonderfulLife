一.需求分析
本项目要求开发一套聊天室软件，该系统包括用户登录、用户注册、显示在线用户、聊天、用户退出、管理员清除聊天内容以及聊天室结束时强制所有在线用户下线功能
1.1用户登录
用户点击登录按钮，首先验证账户密码。然后判断是否在聊天室开放的时间内，管理员可以在任何时间进入系统。
1.2用户注册
用户注册需要填写账号、密码、性别(单选男或女)、昵称。要求账号和密码在5-12位之间，昵称长度在10个汉字以下(具体看数据库设计)。
1.3实时显示在线用户
可以实时看到当前所有在线的用户。
1.4聊天
用户可以发送文字、图片、设置发送文字的颜色，以及显示其他用户的聊天内容。
1.5用户退出
点击“退出”按钮可以退出聊天室，返回登录界面（要求通过浏览器回退按钮返回上个页面系统提示用户尚未登录，请先登录，直接通过地址栏访问项目内容也弹出此提示，并且重定向至登录界面）。
1.6管理员清除聊天内容
管理员登录后，显示独有的“清除聊天内容”按钮，删除所有聊天内容
1.7强制下线
当达到聊天室规定的结束时间，强制所有在线用户下线

二.数据库设计
用户信息表：
create table t_userInfo(
    account varchar(12) not null primary key,
    password varchar(12) not null,
    type int not null,--用户类型，值等于0为普通用户，1为管理员
    online int not null,--用户在线状态，0为不在线，1为在线
    sex char(2) not null check(sex in ('男','女')),
    nickname varchar(20) not null
)
聊天信息表：
create table t_chat(
    id int not null primary key identity(1,1),--消息id
    info varchar(255) not null,--发送信息内容
    time varchar(20) not null,
    sender varchar(12) not null reference t_userInfo(account),--发送人
    color varchar(20) not null --发送信息文字的颜色
) 

三.项目结构(没有则创建)
chat.jsp聊天室主界面
chatInfo.jsp显示聊天内容页面（用frame嵌套到主界面）
login.jsp登录页面
register.jsp注册页面
logout.jsp系统退出页面
notice.jsp公告页面(嵌套)
online.jsp在线用户列表页面(嵌套)
com.edu.seiryo.wonderfulLife.dao包中的类用于与数据库交互
com.edu.seiryo.wonderfulLife.pojo包中的类为实体类，与数据库表对应
com.edu.seiryo.wonderfulLife.servlet包中的类用于处理业务逻辑和控制器的作用
com.edu.seiryo.wonderfulLife.util包中的类为系统工具类
css目录存放css
images目录存放图片
js目录存放js文件（js代码可以用jQuery写）
jsp目录存放所有jsp文件